<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BookOfJob</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Noto+Sans+KR:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #0e0e0e;
      --surface: #161616;
      --border: #2a2a2a;
      --accent: #c9a96e;
      --accent2: #e8c98a;
      --text: #f0ede6;
      --muted: #888;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Noto Sans KR', sans-serif;
      font-weight: 300;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* 배경 패턴 */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        radial-gradient(ellipse 60% 40% at 80% 20%, rgba(201,169,110,0.06) 0%, transparent 60%),
        radial-gradient(ellipse 40% 60% at 20% 80%, rgba(201,169,110,0.04) 0%, transparent 60%);
      pointer-events: none;
      z-index: 0;
    }

    /* 네비게이션 */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      padding: 24px 48px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 0.5px solid var(--border);
      background: rgba(14,14,14,0.85);
      backdrop-filter: blur(12px);
      z-index: 100;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 20px;
      color: var(--accent);
      letter-spacing: 0.05em;
    }

    .nav-links {
      display: flex;
      gap: 36px;
      list-style: none;
    }

    .nav-links a {
      color: var(--muted);
      text-decoration: none;
      font-size: 13px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      transition: color 0.3s;
    }

    .nav-links a:hover { color: var(--accent); }

    /* 히어로 섹션 */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 120px 48px 80px;
      position: relative;
      z-index: 1;
    }

    .hero-content {
      max-width: 700px;
      animation: fadeUp 0.9s ease both;
    }

    .hero-label {
      font-size: 11px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 28px;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .hero-label::before {
      content: '';
      display: inline-block;
      width: 32px;
      height: 0.5px;
      background: var(--accent);
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(48px, 7vw, 88px);
      font-weight: 400;
      line-height: 1.1;
      margin-bottom: 28px;
      color: var(--text);
    }

    h1 em {
      font-style: italic;
      color: var(--accent);
    }

    .hero-desc {
      font-size: 16px;
      color: var(--muted);
      line-height: 1.8;
      max-width: 480px;
      margin-bottom: 48px;
    }

    .btn-group {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }

    .btn-primary {
      padding: 14px 32px;
      background: var(--accent);
      color: #0e0e0e;
      border: none;
      font-family: 'Noto Sans KR', sans-serif;
      font-size: 13px;
      font-weight: 500;
      letter-spacing: 0.06em;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.3s, transform 0.2s;
      display: inline-block;
    }

    .btn-primary:hover {
      background: var(--accent2);
      transform: translateY(-2px);
    }

    .btn-outline {
      padding: 14px 32px;
      background: transparent;
      color: var(--text);
      border: 0.5px solid var(--border);
      font-family: 'Noto Sans KR', sans-serif;
      font-size: 13px;
      letter-spacing: 0.06em;
      cursor: pointer;
      text-decoration: none;
      transition: border-color 0.3s, color 0.3s;
      display: inline-block;
    }

    .btn-outline:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    /* 숫자 섹션 */
    .stats {
      position: absolute;
      right: 48px;
      bottom: 80px;
      display: flex;
      gap: 48px;
      animation: fadeUp 1.1s ease both;
    }

    .stat-item { text-align: right; }

    .stat-num {
      font-family: 'Playfair Display', serif;
      font-size: 36px;
      color: var(--accent);
      display: block;
    }

    .stat-label {
      font-size: 11px;
      color: var(--muted);
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    /* 카드 섹션 */
    .section {
      padding: 100px 48px;
      position: relative;
      z-index: 1;
    }

    .section-header {
      display: flex;
      align-items: baseline;
      gap: 20px;
      margin-bottom: 56px;
    }

    .section-num {
      font-family: 'Playfair Display', serif;
      font-size: 13px;
      color: var(--accent);
    }

    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: 36px;
      font-weight: 400;
      color: var(--text);
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1px;
      background: var(--border);
      border: 0.5px solid var(--border);
    }

    .card {
      background: var(--surface);
      padding: 40px 36px;
      transition: background 0.3s;
    }

    .card:hover { background: #1e1e1e; }

    .card-icon {
      width: 40px;
      height: 40px;
      border: 0.5px solid var(--accent);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 24px;
      color: var(--accent);
      font-size: 18px;
    }

    .card-title {
      font-family: 'Playfair Display', serif;
      font-size: 20px;
      font-weight: 400;
      margin-bottom: 12px;
      color: var(--text);
    }

    .card-body {
      font-size: 14px;
      color: var(--muted);
      line-height: 1.8;
    }

    /* 구분선 */
    .divider {
      border: none;
      border-top: 0.5px solid var(--border);
      margin: 0 48px;
    }

    /* 푸터 */
    footer {
      padding: 40px 48px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 0.5px solid var(--border);
      position: relative;
      z-index: 1;
    }

    .footer-logo {
      font-family: 'Playfair Display', serif;
      font-size: 16px;
      color: var(--accent);
    }

    .footer-copy {
      font-size: 12px;
      color: var(--muted);
      letter-spacing: 0.05em;
    }

    /* 애니메이션 */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .card {
      animation: fadeUp 0.7s ease both;
    }

    .card:nth-child(1) { animation-delay: 0.1s; }
    .card:nth-child(2) { animation-delay: 0.2s; }
    .card:nth-child(3) { animation-delay: 0.3s; }

    @media (max-width: 768px) {
      nav { padding: 20px 24px; }
      .nav-links { display: none; }
      .hero { padding: 100px 24px 60px; }
      .stats { position: static; margin-top: 48px; justify-content: flex-start; }
      .section { padding: 60px 24px; }
      footer { padding: 32px 24px; flex-direction: column; gap: 12px; text-align: center; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="logo">BookOfJob</div>
    <ul class="nav-links">
      <li><a href="#">소개</a></li>
      <li><a href="#">프로젝트</a></li>
      <li><a href="#">연락처</a></li>
    </ul>
  </nav>

  <section class="hero">
    <div class="hero-content">
      <div class="hero-label">포트폴리오</div>
      <h1>나만의<br><em>이야기</em>를<br>담다</h1>
      <p class="hero-desc">
        이곳은 나의 작업물과 아이디어를 기록하는 공간입니다.<br>
        GitHub로 관리되는 나의 첫 번째 프로젝트.
      </p>
      <div class="btn-group">
        <a href="#" class="btn-primary">프로젝트 보기</a>
        <a href="#" class="btn-outline">연락하기</a>
      </div>
    </div>

    <div class="stats">
      <div class="stat-item">
        <span class="stat-num">01</span>
        <span class="stat-label">첫 번째 커밋</span>
      </div>
      <div class="stat-item">
        <span class="stat-num">∞</span>
        <span class="stat-label">가능성</span>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section class="section">
    <div class="section-header">
      <span class="section-num">01</span>
      <h2 class="section-title">무엇을 할 수 있나요</h2>
    </div>
    <div class="cards">
      <div class="card">
        <div class="card-icon">✦</div>
        <div class="card-title">웹 개발</div>
        <div class="card-body">HTML, CSS, JavaScript를 활용한 웹페이지 제작. Bubble로 빠르게 프로토타입을 만들고 GitHub로 관리합니다.</div>
      </div>
      <div class="card">
        <div class="card-icon">◈</div>
        <div class="card-title">프로젝트 관리</div>
        <div class="card-body">GitHub를 통해 코드 변경 이력을 관리하고, 팀원과 협업하며 체계적으로 프로젝트를 운영합니다.</div>
      </div>
      <div class="card">
        <div class="card-icon">◎</div>
        <div class="card-title">아이디어 실현</div>
        <div class="card-body">머릿속의 아이디어를 실제 작동하는 서비스로 만들어냅니다. 작은 시작이 큰 변화를 만듭니다.</div>
      </div>
    </div>
  </section>

  <footer>
    <div class="footer-logo">BookOfJob</div>
    <div class="footer-copy">© 2026 — GitHub로 관리되는 나의 첫 프로젝트</div>
  </footer>

</body>
</html>